<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sicnu.dao.Check_ProjectDao">

    <insert id="addProject" parameterType="Check_Project">
        insert into check_project(
        project_stage,
        project_type,
        leader_id,
        project_name,
        project_abstract,
        declaration,
        apply_time
        ) values(
        #{project_stage},
        #{project_type},
        #{leader_id},
        #{project_name},
        #{project_abstract},
        #{declaration},
        #{apply_time}
        )
    </insert>

    <select id="findAllProject" parameterType="string" resultType="Project">
		select * from check_project
	</select>


</mapper>