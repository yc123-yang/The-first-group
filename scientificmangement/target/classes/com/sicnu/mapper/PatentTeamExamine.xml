<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sicnu.mapper.PatentTeamExamineMapper">

    <insert id="addPatentTeamExamineUser" parameterType="PatentTeamExamine">
        insert into patent_team_examine
        (
        patent_id,
        user_id,
        user_role,
        contribution
        )
        values
        (
        #{patent_id},
        #{user_id},
        #{user_role},
        #{contribution}
        )
    </insert>

    <delete id="delPatentTeamExamineTeam" parameterType="int">
        delete from patent_team_examine
        where
        patent_id = #{patent_id}
    </delete>

    <select id="selectPatentTeamExamineById" parameterType="int">
        select * from patent_team_examine
        where
        patent_id = #{patent_id}
    </select>


</mapper>